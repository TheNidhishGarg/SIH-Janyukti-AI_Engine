# --- Web ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# --- Config & validation ---
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1
python-dotenv>=1.0.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
# asyncpg>=0.29.0        # uncomment for PostgreSQL in deployment

# --- Auth ---
pyjwt>=2.8.0
passlib>=1.7.4

# --- ML ---
numpy>=1.24.0
google-generativeai>=0.4.0
# Optional: the embedding model. Without it the hashing fallback is used and
# everything still runs (see app/ml/embeddings.py).
sentence-transformers>=2.5.0

# --- Media (optional) ---
cloudinary>=1.39.0

# --- Dev ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
